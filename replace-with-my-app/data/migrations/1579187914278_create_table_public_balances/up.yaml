- args:
    sql: CREATE TABLE "public"."balances"("account_balance" numeric NOT NULL, "due_now"
      numeric NOT NULL, "patient_id" uuid NOT NULL, "payer_id" uuid NOT NULL, PRIMARY
      KEY ("patient_id","payer_id") ); COMMENT ON TABLE "public"."balances" IS E'derived
      from core/FS';
  type: run_sql
- args:
    name: balances
    schema: public
  type: add_existing_table_or_view
