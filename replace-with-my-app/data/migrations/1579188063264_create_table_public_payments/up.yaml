- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."payments"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "created_at" timestamptz NOT NULL DEFAULT now(), "payer_id" uuid NOT NULL, "patient_id"
      uuid NOT NULL, "amount" numeric NOT NULL, "type" text NOT NULL, "context" jsonb
      NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: payments
    schema: public
  type: add_existing_table_or_view
